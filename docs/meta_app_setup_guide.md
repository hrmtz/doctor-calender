# Meta Developers アプリ作成・審査申請 手順書

## 前提条件

- Facebookビジネスアカウント保有
- Instagramビジネスアカウント（クリニック公式アカウント）
- Facebookビジネスマネージャーアカウント

---

## Step 1: developers.facebook.com でアプリ作成

1. [https://developers.facebook.com/](https://developers.facebook.com/) にアクセス
2. 右上の「マイアプリ」をクリック → 「アプリを作成」
3. アプリタイプを選択:
   - **「ビジネス」** を選択（Instagram API利用に推奨）
4. アプリ情報を入力:
   - **アプリ表示名**: `Doctor Calendar` （任意のアプリ名）
   - **アプリの連絡先メールアドレス**: クリニックの管理者メールアドレス
   - **ビジネスアカウント**: 事前作成したFacebookビジネスアカウントを選択
5. 「アプリを作成」をクリック

> **📸 スクリーンショット取得**: アプリダッシュボード画面（App ID が表示されている状態）

---

## Step 2: Instagram製品を追加

1. アプリダッシュボードの左メニュー「製品を追加」をクリック
2. 一覧から **「Instagram」** を探し「設定」をクリック
3. Instagram製品がアプリに追加されたことを確認
4. 左メニューに「Instagram」が追加される

> **📸 スクリーンショット取得**: Instagram製品追加後のダッシュボード（左メニューにInstagramが表示されている状態）

---

## Step 3: テストモードでAPI動作確認

### 3-1. テストユーザーの追加

1. アプリダッシュボード → 「アプリの役割」→「ロール」
2. 「テスターを追加」: Instagramビジネスアカウントを持つFacebookユーザーを追加
3. 追加されたユーザーがInstagramアプリ側で招待を承認する

### 3-2. アクセストークンの取得（テスト用）

1. 左メニュー「Instagram」→「API設定」→「Instagram基本表示API」
2. 「新しいトークンを生成」でテストユーザーのトークンを取得
3. または **Graph API Explorer** ([https://developers.facebook.com/tools/explorer/](https://developers.facebook.com/tools/explorer/)) を使用:
   - アプリを選択 → User Token生成
   - スコープ: `instagram_business_basic`, `instagram_business_content_publish` を選択

> **📸 スクリーンショット取得**: Graph API Explorerでアクセストークン生成画面

### 3-3. テストAPI呼び出し

```bash
# ユーザー情報取得テスト
curl -X GET \
  "https://graph.facebook.com/v18.0/me?fields=id,name&access_token={アクセストークン}"

# Instagramビジネスアカウント情報取得
curl -X GET \
  "https://graph.facebook.com/v18.0/{ig-user-id}?fields=id,username,followers_count&access_token={アクセストークン}"

# Stories投稿テスト（画像コンテナ作成）
curl -X POST \
  "https://graph.facebook.com/v18.0/{ig-user-id}/media" \
  -d "media_type=STORIES" \
  -d "image_url={公開アクセス可能な画像URL}" \
  -d "access_token={アクセストークン}"
```

> **📸 スクリーンショット取得**: API呼び出し成功レスポンス（`id` または `creation_id` が返ってくる状態）

---

## Step 4: 審査申請

### 4-1. 審査申請前チェックリスト

- [ ] アプリのプライバシーポリシーURL が設定済み
- [ ] アプリのサービス利用規約URL が設定済み
- [ ] アプリアイコン（1024×1024 px）がアップロード済み
- [ ] アプリ説明文（英語）が記入済み
- [ ] デモ動画または使用方法の説明が準備済み
- [ ] テストモードでのAPI動作確認が完了済み

### 4-2. プライバシーポリシーとデータ使用の設定

1. ダッシュボード → 「設定」→「基本設定」
2. 以下を設定:
   - **プライバシーポリシーURL**: クリニックのサイト上のポリシーページURL
   - **利用規約URL**: 同上
   - **ユーザーデータ削除URL または手順**: データ削除リクエスト対応フォームのURL

> **📸 スクリーンショット取得**: 基本設定画面（URL設定済みの状態）

### 4-3. パーミッション申請

1. 左メニュー「App Review」→「権限と機能」
2. 審査対象パーミッションを選択:
   - **`instagram_business_basic`**: 「リクエスト」ボタンをクリック
   - **`instagram_business_content_publish`**: 「リクエスト」ボタンをクリック
3. 各パーミッションの使用目的を英語で記入（`permission_justification.md` 参照）
4. デモ動画またはスクリーンキャストをアップロード（`meta_review_screencast.md` 参照）

> **📸 スクリーンショット取得**: 権限申請フォーム（使用目的記入済みの状態）

### 4-4. 審査送信

1. 全項目の入力・アップロードが完了したら「審査のため送信」をクリック
2. Metaからの審査結果はアプリ登録メールアドレスに通知される（通常1〜5営業日）
3. 追加情報を求められた場合は、Metaからのメール指示に従い補足資料を提出

---

## 参考リンク

- [Instagram Graph API ドキュメント](https://developers.facebook.com/docs/instagram-api)
- [Content Publishing API](https://developers.facebook.com/docs/instagram-api/guides/content-publishing)
- [App Review ガイドライン](https://developers.facebook.com/docs/app-review)
- [Graph API Explorer](https://developers.facebook.com/tools/explorer/)
